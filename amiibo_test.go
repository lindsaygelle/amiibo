package amiibo_test

import (
	"testing"
)

func Test(t *testing.T) {
	/*a, err := amiibo.Get()
	if err != nil {
		panic(err)
	}
	m, err := amiibo.NewMap("Name", a...)
	if err != nil {
		panic(err)
	}
	http.ListenAndServe(":80", amiibo.Server(m))
	*/
}
