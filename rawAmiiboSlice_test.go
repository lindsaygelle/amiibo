package amiibo

import (
	"testing"
)

func TestRawAmiiboSlice(t *testing.T) {
	/*b, _ := local()
	slice := getRawAmiiboSlice(b)

	slice.Each(func(i int, rawAmiibo *RawAmiibo) {
		fmt.Println(rawAmiibo.HexCode, rawAmiibo.AmiiboName)
	})*/
}
